# 工單：回測研究有效性修復（執行方：agy / Gemini）

你是這張工單的唯一執行者，這份檔案是你唯一的指令來源。工作目錄 `C:/CLAUDE/專案-投資/tw-stock-db/`。全程繁體中文。
**這張工單不新增任何策略、不調任何門檻。** 目的只有一個：把 Codex 審查（`docs/tasks/reports/codex-design-review.md`，請先完整讀一遍）列為【嚴重】的量測問題修掉，然後重新產出同期配對比較，讓既有結論能被正確地打折或推翻。

## 修復項目（全部要做，逐項在報告中對應）

### A. 訊號時點與成交時點分離
- 訊號只在**完整交易月**的月末產生：若資料最後一天不是該月最後交易日（例如 2026-09-04），該月不產生訊號，也不作為任何前瞻報酬的終點。
- 進場價改為**訊號日的下一個交易日收盤**；出場價為到期月月末的下一個交易日收盤（同樣延一天，保持持有長度）。訊號計算（動能、rank、PER、法人）仍用訊號日及之前的資料。
- 報告要並列「舊：同日收盤成交」與「新：次日收盤成交」的差異。

### B. 公司行動還原
- 用 `fm_price_daily` 偵測持有期內的分割／減資：**只認向下跳動**，即 close/prev < 0.6 且 prev/close 接近常見分割或減資比例（2、3、4、5、10 倍，或減資常見的 1.25–2 倍，容許 ±5%）才視為事件；向上跳動、以及倍率不像整數比的向下跳動（新股首五日無漲跌幅、重大利空、暫停交易後開盤）一律視為真實報酬，不得抹平。該檔該期報酬改以**日報酬連乘還原、跳過事件日那一筆**（事件日報酬記 0，即視為股數等比調整；數學上等價於把事件日之後的價格乘上倍率做除權還原序列，**不是**前段未除權價與後段除權價直接相除）。同時把偵測到的事件寫成 `backtest/corporate_actions_detected.csv`（股號、日期、prev、close、倍率），報告需列出事件數與對整體超額的影響（含／不含兩版），供人工對公開資訊觀測站核對；人工核對是事後步驟，不阻斷回測執行。
- 動能視窗與 PER 歷史若跨過事件日，該檔該月動能記 NA、PER 位置記 NA（不硬算）。

### C. 持有期報酬與缺值語意
- 每檔部位保存訊號日成員與初始權重；到期缺價（停牌／下市）不再刪除或當 0，而是用最後可得收盤價結算並在 detail 標記 `exit_reason=last_available`；報告列出這類部位數量與對整體超額的影響（含／不含兩版）。
- 前瞻端點不得往未來搜尋超過到期日；只允許往前找最近交易日。

### D. 統一母體與缺值
- 兩支回測（`backtest_valuation.py`、`backtest_t1.py`）改用同一個 `eligibility` 函式：該月要有訊號日收盤、且訊號日前 250 個交易日內至少 200 筆收盤。C1 在兩支程式中必須是同一個池（命名 `C1`），估值層另外要求 PER 可算時，記為 `C1_PER` 並各自報數。
- EPS 八季、營收三月要求**連續**（缺季／缺月即 NA），並限制最新一筆距訊號日不超過 200 天（EPS）／45 天（營收）。
- 法人：無資料回 NA（不是 0），T1b／T1／T1x 對 NA 一律不觸發；G1 廣度暖機期回 NA。
- 母體清單凍結：從 `valuation_screen` 取 `run_date = 2026-09-04` 那一次的 stock_id 存成 `backtest/universe_frozen_2026-09-04.csv`（股號、universe、sub），兩支程式只讀這個檔。

### E. 組合層重算
- `generate_portfolio_equity_curve`：三個 cohort 各自買入持有三個月（權重隨價格漂移，不每月重設），cohort 間資金按實際到期再投入；成本按實際買入與賣出金額各計 0.3%（合計 0.6% 一進一出），同一檔跨 cohort 先淨額再計費。
- 暖機期（策略無選股）不扣成本；報告要列各策略「有部位月份數／總月份數」。
- 年化只用完整月份。

### F. 同期配對比較（核心產出）
- 共同期間 = 所有比較策略都有有效訊號的月份交集。
- 對每組父子策略（D0→D2、D2→D3、D3→D5、C1→T1x、L0→L1→L2）算**同月配對差**：平均、中位、勝率，並用**時間區塊 bootstrap**（區塊長 6 個月、重抽 2000 次、所有策略共用同一抽樣索引）給 95% 信賴區間。
- 補一個固定對照 `MOM_12_1`：t−1 月末 / t−12 月末 − 1（排除最近一個月），同池、同成交規則，只報它對 universe 與對 C1 的配對差，不做其他變體。
- 產出 `backtest/validity_summary.md`：修復前後的主要數字對照表（D5、C1、L1 的 6 個月勝率與超額）、配對差與信賴區間表、公司行動與缺值影響表、以及一段「哪些既有結論在修復後仍成立、哪些不成立」。

## 測試（`tests/test_validity.py`，全綠）
(a) 不完整月份不產生訊號；(b) 進場價為次日收盤；(c) 分割還原後報酬正確（假資料一拆三，經濟報酬 0）；(d) 缺價部位用最後可得價且有標記；(e) 連續八季檢查（缺一季→NA）；(f) 法人缺資料→NA 不觸發；(g) 區塊 bootstrap 抽樣索引在策略間一致；(h) 兩支程式的 C1 同月成員完全相同。既有測試不得刪改；若因語意修正而必須調整既有測試，逐一在報告列出理由。

## 產出
`backtest_valuation.py`、`backtest_t1.py`（可改）、`backtest/validity_summary.md`、`backtest/corporate_actions_detected.csv`、`backtest/universe_frozen_2026-09-04.csv`、`tests/test_validity.py`、`docs/tasks/reports/backtest-validity-report.md`（做了什麼、每個修復項對應的行號、修復前後對照、不確定處）。

## 禁止
- 不打 FinMind、不寫 DB、不動 `build_*.py`、HANDOFF.md、README.md。
- 不 git commit / push。
- 不新增策略、不調門檻、不改 D／T1／G1 的定義（只修量測）。
